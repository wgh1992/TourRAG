# Core dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Database
psycopg2-binary==2.9.9
sqlalchemy==2.0.23
alembic==1.12.1

# LLM & MCP
openai==1.3.5
# Note: MCP (Model Context Protocol) is a concept, not a Python package dependency
# The system implements MCP-style tools but doesn't require the mcp package

# Image processing (for visual tag extraction)
Pillow==10.1.0
# torch and transformers are optional - only needed if using local vision models
# torch>=2.2.0  # Uncomment if needed
# transformers>=4.35.0  # Uncomment if needed
exifread==3.1.0  # For reading EXIF metadata including GPS coordinates
requests==2.31.0  # For downloading images from Wikimedia Commons

# Utilities
python-dotenv==1.0.0
httpx==0.25.1
python-multipart==0.0.6

# Visualization (for distribution maps)
matplotlib==3.8.2
pandas==2.1.4
geopandas==0.14.1

# Geocoding (optional - for country information extraction)
# Install these for better geocoding performance and offline support:
# reverse-geocoder>=1.6.1  # Offline reverse geocoding (recommended, fastest)
# geopy>=2.4.0  # Multiple geocoding providers (alternative online method)
# pycountry>=23.12.0  # Country code to name conversion (recommended with reverse-geocoder)

# Development
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0
mypy==1.7.0

